import styled from 'styled-components';

const Nav = styled.ul`
`

const NavItem = styled.li`
    margin: 24px;

    a {
        color: rgb(33, 107, 235);
        text-decoration: none;
    }    
`

export { NavItem, Nav }

